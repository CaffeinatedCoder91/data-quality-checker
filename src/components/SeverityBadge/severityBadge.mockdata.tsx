import type { SeverityBadgeProps } from './severityBadge.types';

export const SeverityBadgeMockData: Record<string, SeverityBadgeProps> = {
  low: {
    severity: 'low',
  },
  medium: {
    severity: 'medium',
  },
  high: {
    severity: 'high',
  },
};
